server:
  port: 8080
  servlet:
    context-path: /familyhealth
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/familyhealth1
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true